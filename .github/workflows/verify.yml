name: Verify Task Board

on:
  push:
  pull_request:

jobs:
  verify:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run app:build
      - run: npm run app:package
      - run: npm run app:verify
      - run: test "$(sha256sum artifacts/task-board.app/application.wasm | cut -d' ' -f1)" = "$(node -e 'const fs=require("node:fs"); const m=JSON.parse(fs.readFileSync("artifacts/task-board.app/manifest","utf8")); process.stdout.write(m.artifact.hash)')"
